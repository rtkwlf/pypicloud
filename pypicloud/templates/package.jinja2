<!DOCTYPE html>
<html>
<head>
  <title>Package Index</title>
</head>
<body>
  {% for filename, pkg_data in pkgs|dictsort %}
  {%- set requires_python_urlencoded = pkg_data.requires_python|escape if pkg_data.requires_python %}
    <a href="{{ pkg_data.url }}"{{ ' data-requires-python="{}"'.format(requires_python_urlencoded)|safe if requires_python_urlencoded -}}>{{ filename }}</a><br>
  {%- endfor %}
</body>
</html>
